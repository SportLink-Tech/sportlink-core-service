version: '3.8'

services:
  app:
    image: sportlink-core-service
    build: .
    ports:
      - "8080:8080"
    environment:
      - GIN_MODE=release
    volumes:
      - .:/app
    restart: unless-stopped